I need to:
1. In `SavingsCalculatorAnalysisPage.tsx`, create a hidden div that loops over `clientOverview.months`.
2. Inside the loop, render the 3 specific components we need to capture:
   - Dashboard Hero + Daily Savings Opportunity + Purchase Comparison (This is what Image 1 & 4 show). Actually, Image 1 shows `<DashboardHero>`, `<DashboardEnergySummary>`, `<DashboardMarketMix>`.
   - `<DashboardMonthlyInsights>` (Image 2).
   - `<DashboardDataTable>` (Image 3).
3. Add IDs to these like `capture-hero-${month.month}`, `capture-insights-${month.month}`, `capture-table-${month.month}`.
4. Use `html2canvas` to capture them all into Base64 strings.
5. In the `monthlyData` array for docxtemplater, add `hero_image`, `insights_image`, `table_image`.
6. Update the Word template to replace the hardcoded "January 2026" and "May 2026" sections with a `<<#monthlyData>>` loop that outputs `{%hero_image}`, `{%insights_image}`, `{%table_image}`.
7. Replace the "MONTHLY SAVINGS OPPORTUNITY" static chart with `{%monthly_savings_chart}` correctly.
8. Replace "Twenty Five lakhs" with `<<savings_in_words>>`.
